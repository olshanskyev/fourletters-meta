{
  "openapi": "3.0.3",
  "info": {
    "title": "Shared Data Models for Fourletters API",
    "version": "1.0.0",
    "description": "Shared data models for the fourletters."
  },
  "paths": {},
  "components": {
    "schemas": {
      "ReceiptType": {
        "type": "string",
        "enum": ["delivered", "read"],
        "description": "'delivered' = the recipient device received and decrypted the message; 'read' = the user opened it."
      },
      "RefreshError": {
        "type": "object",
        "required": ["reason"],
        "description": "Body of a 401 from /auth/refresh. 'revoked' means a newer login took over the single active device, on which the client wipes its local E2E identity keys; the other reasons just require re-authentication.",
        "properties": {
          "reason": {
            "type": "string",
            "enum": ["revoked", "expired", "invalid"],
            "description": "'revoked' = invalidated by a newer login (single-active-device takeover); 'expired' = refresh token past expiry; 'invalid' = malformed/missing or session mismatch."
          }
        }
      },
      "AuthRequest": {
        "type": "object",
        "required": ["token"],
        "properties": {
          "token": {
            "type": "string",
            "description": "The identity token provided by the third-party OAuth provider."
          }
        }
      },
      "AuthResponse": {
        "type": "object",
        "required": ["access_token", "user"],
        "properties": {
          "access_token": {
            "type": "string",
            "description": "The short-lived JWT Access Token."
          },
          "user": {
            "$ref": "#/components/schemas/UserResponse"
          }
        }
      },
      "UserResponse": {
        "type": "object",
        "required": ["id", "username", "roles"],
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "username": {
            "type": "string"
          },
          "avatarUrl": {
            "type": "string",
            "format": "uri"
          },
          "roles": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "List of user permissions/roles for RBAC."
          }
        }
      },
      "EncryptedMessage": {
        "type": "object",
        "required": ["messageId", "recipientId", "payload", "signature"],
        "properties": {
          "messageId": {
            "type": "string",
            "format": "uuid",
            "description": "Unique identifier for the message generated by the client"
          },
          "recipientId": {
            "type": "string",
            "format": "uuid",
            "description": "ID of the recipient user"
          },
          "senderId": {
            "type": "string",
            "format": "uuid",
            "description": "ID of the sender user. Set by the Server from the authenticated session at send time; any client-provided value is ignored. Omitted in the send request."
          },
          "payload": {
            "type": "string",
            "description": "E2E encrypted message payload (Base64). For a 1:1 message this is per-message ephemeral-ECDH ciphertext; for a group message it is ciphertext under the group's symmetric epoch key."
          },
          "signature": {
            "type": "string",
            "description": "Detached signature over the payload produced by the sender's identity key (Base64). Verified end-to-end by the recipient against the public-key directory."
          },
          "groupId": {
            "type": "string",
            "format": "uuid",
            "description": "Present only for group messages. Identifies the group; the Server uses it to resolve the roster and fan out one stored copy per member. Omitted for 1:1 messages."
          },
          "epoch": {
            "type": "integer",
            "format": "int64",
            "description": "Present only for group messages. The group-key epoch (version) the payload was encrypted under, so the recipient selects the matching key. Omitted for 1:1 messages."
          }
        }
      },
      "AcceptedResponse": {
        "type": "object",
        "required": ["messageId", "status"],
        "properties": {
          "messageId": {
            "type": "string",
            "format": "uuid",
            "description": "ID of the accepted message (echoed from the request)."
          },
          "status": {
            "type": "string",
            "enum": ["accepted"],
            "description": "The Server has durably taken ownership of the message."
          },
          "serverStartedAt": {
            "type": "integer",
            "format": "int64",
            "description": "Epoch milliseconds at which this Server process started. A changed value across responses signals a restart, after which the client re-submits its unconfirmed outbox messages."
          }
        }
      },
      "InboxResponse": {
        "type": "object",
        "required": ["messages"],
        "properties": {
          "messages": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/EncryptedMessage"
            },
            "description": "The recipient's undelivered messages, in arrival order."
          },
          "receipts": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/MessageReceipt"
            },
            "description": "Pending delivery/read acknowledgements for messages this user sent, accumulated while the user was offline. Returned once and then cleared."
          },
          "groupKeys": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/GroupKeySet"
            },
            "description": "Wrapped group keys the caller is missing for groups they belong to — e.g. they were offline during a rotation or just joined. Each entry is the current epoch's key wrapped to the caller's encryption key. Folded into the inbox pull so reconnect is a single round-trip. Returned once and then cleared."
          },
          "serverStartedAt": {
            "type": "integer",
            "format": "int64",
            "description": "Epoch milliseconds at which this Server process started. A changed value across responses signals a restart, after which the client re-submits its unconfirmed outbox messages."
          }
        }
      },
      "MessageReceipt": {
        "type": "object",
        "required": ["messageId", "recipientId", "type", "signature"],
        "description": "A delivery/read acknowledgement for a message the calling user sent, delivered to the sender via the inbox pull.",
        "properties": {
          "messageId": {
            "type": "string",
            "format": "uuid",
            "description": "ID of the original message being acknowledged."
          },
          "recipientId": {
            "type": "string",
            "format": "uuid",
            "description": "ID of the user who delivered/read the message."
          },
          "type": {
            "$ref": "#/components/schemas/ReceiptType"
          },
          "signature": {
            "type": "string",
            "description": "Signature over (messageId, type, originalSenderId) by the recipient's identity key (Base64), echoed unaltered from the submitted DeliveryReceipt."
          }
        }
      },
      "MessageBatchRequest": {
        "type": "object",
        "required": ["messages"],
        "description": "Submits several messages in one call — used by the client to re-submit unconfirmed outbox messages after a detected Server restart. Idempotent per messageId.",
        "properties": {
          "messages": {
            "type": "array",
            "minItems": 1,
            "maxItems": 100,
            "items": {
              "$ref": "#/components/schemas/EncryptedMessage"
            },
            "description": "The messages to (re)submit, capped per request; the client chunks larger resyncs."
          }
        }
      },
      "MessageBatchResponse": {
        "type": "object",
        "required": ["results", "serverStartedAt"],
        "properties": {
          "results": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/AcceptedResponse"
            },
            "description": "Per-message acceptance result, in request order."
          },
          "serverStartedAt": {
            "type": "integer",
            "format": "int64",
            "description": "Epoch milliseconds at which this Server process started (carried once for the whole batch)."
          }
        }
      },
      "DeliveryReceipt": {
        "type": "object",
        "required": ["messageId", "type", "signature"],
        "properties": {
          "messageId": {
            "type": "string",
            "format": "uuid",
            "description": "ID of the message being acknowledged."
          },
          "originalSenderId": {
            "type": "string",
            "format": "uuid",
            "description": "ID of the original message's sender — the party to notify. Echoed by the recipient from the received message so the Server can relay this receipt "
          },
          "type": {
            "$ref": "#/components/schemas/ReceiptType"
          },
          "signature": {
            "type": "string",
            "description": "Signature over (messageId, type, originalSenderId) by the recipient's identity key (Base64). Lets the original sender trust the receipt without trusting any Hub."
          }
        }
      },
      "KeysUploadRequest": {
        "type": "object",
        "required": ["signingPublicKey", "encryptionPublicKey"],
        "properties": {
          "signingPublicKey": {
            "type": "string",
            "description": "The caller's ECDSA P-256 public signing key (Base64 SPKI)."
          },
          "encryptionPublicKey": {
            "type": "string",
            "description": "The caller's ECDH P-256 public encryption key (Base64 SPKI)."
          }
        }
      },
      "PublicKeySet": {
        "type": "object",
        "required": ["signingPublicKey", "encryptionPublicKey", "uploadedAt"],
        "properties": {
          "signingPublicKey": {
            "type": "string",
            "description": "ECDSA P-256 public signing key (Base64 SPKI)."
          },
          "encryptionPublicKey": {
            "type": "string",
            "description": "ECDH P-256 public encryption key (Base64 SPKI)."
          },
          "uploadedAt": {
            "type": "integer",
            "format": "int64",
            "description": "Epoch milliseconds when these public keys were uploaded to the directory."
          }
        }
      },
      "KeysResponse": {
        "type": "object",
        "required": ["userId", "keys"],
        "properties": {
          "userId": {
            "type": "string",
            "format": "uuid",
            "description": "The user id these public keys belong to."
          },
          "keys": {
            "$ref": "#/components/schemas/PublicKeySet"
          }
        }
      },
      "PublicKeysBatchResponse": {
        "type": "object",
        "required": ["results"],
        "properties": {
          "results": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/KeysResponse"
            }
          }
        }
      },
      "ReceiptData": {
        "type": "object",
        "required": ["messageId", "recipientId", "type", "signature"],
        "properties": {
          "messageId": {
            "type": "string",
            "format": "uuid",
            "description": "ID of the original message this receipt refers to."
          },
          "recipientId": {
            "type": "string",
            "format": "uuid",
            "description": "ID of the user who delivered/read the message (the original recipient)."
          },
          "type": {
            "$ref": "#/components/schemas/ReceiptType"
          },
          "signature": {
            "type": "string",
            "description": "Signature over (messageId, type, originalSenderId) by the recipient's identity key (Base64), relayed unaltered from the submitted DeliveryReceipt."
          }
        }
      },
      "MessageEvent": {
        "type": "object",
        "required": ["event", "data"],
        "description": "WS push: a new encrypted message relayed live by the Hub.",
        "properties": {
          "event": {
            "type": "string",
            "enum": ["messageReceived"]
          },
          "data": {
            "$ref": "#/components/schemas/EncryptedMessage"
          }
        }
      },
      "ReceiptEvent": {
        "type": "object",
        "required": ["event", "data"],
        "description": "WS push: a delivery/read notification relayed live to the original sender.",
        "properties": {
          "event": {
            "type": "string",
            "enum": ["messageDelivered", "messageRead"]
          },
          "data": {
            "$ref": "#/components/schemas/ReceiptData"
          }
        }
      },
      "GroupKeyEvent": {
        "type": "object",
        "required": ["event", "data"],
        "description": "WS push: a content-free nudge that a group's key rotated to a new epoch. The member refetches its wrapped key (live blob, GET /groups/{id}/keys, or the next /inbox groupKeys pull). The group key itself is never carried over this channel.",
        "properties": {
          "event": {
            "type": "string",
            "enum": ["groupKeyRotated"]
          },
          "data": {
            "$ref": "#/components/schemas/GroupKeyNotification"
          }
        }
      },
      "GroupKeyNotification": {
        "type": "object",
        "required": ["groupId", "epoch"],
        "description": "Payload of a groupKeyRotated WS event.",
        "properties": {
          "groupId": {
            "type": "string",
            "format": "uuid",
            "description": "The group whose key rotated."
          },
          "epoch": {
            "type": "integer",
            "format": "int64",
            "description": "The new current epoch."
          }
        }
      },
      "GroupMember": {
        "type": "object",
        "required": ["userId"],
        "description": "A member of a group.",
        "properties": {
          "userId": {
            "type": "string",
            "format": "uuid",
            "description": "The member's user id."
          },
          "joinedAt": {
            "type": "integer",
            "format": "int64",
            "description": "Epoch milliseconds when the member joined the group."
          }
        }
      },
      "Group": {
        "type": "object",
        "required": ["id", "name", "ownerId", "epoch", "members"],
        "description": "A group conversation. The Server owns the roster and the current epoch number but never the group key (it only relays opaque wrapped key blobs).",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "description": "Server-assigned group identifier."
          },
          "name": {
            "type": "string",
            "description": "Human-readable group name (stored in plaintext server-side)."
          },
          "ownerId": {
            "type": "string",
            "format": "uuid",
            "description": "The group owner — the sole party permitted to change the roster and publish rotations."
          },
          "epoch": {
            "type": "integer",
            "format": "int64",
            "description": "The current group-key epoch (version). Incremented on every rotation (membership change or owner-initiated rekey)."
          },
          "members": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/GroupMember"
            },
            "description": "The current roster."
          },
          "createdAt": {
            "type": "integer",
            "format": "int64",
            "description": "Epoch milliseconds when the group was created."
          },
          "updatedAt": {
            "type": "integer",
            "format": "int64",
            "description": "Epoch milliseconds of the last roster/epoch change."
          }
        }
      },
      "GroupSummary": {
        "type": "object",
        "required": ["id", "name", "ownerId", "epoch"],
        "description": "Lightweight group descriptor for list views.",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "name": {
            "type": "string"
          },
          "ownerId": {
            "type": "string",
            "format": "uuid"
          },
          "epoch": {
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "WrappedGroupKey": {
        "type": "object",
        "required": ["recipientId", "wrappedKey"],
        "description": "The group's symmetric epoch key wrapped (encrypted) to a single member's encryption public key. The Server stores and relays this opaque blob but never the group key itself.",
        "properties": {
          "recipientId": {
            "type": "string",
            "format": "uuid",
            "description": "The member this blob is encrypted for."
          },
          "wrappedKey": {
            "type": "string",
            "description": "The group epoch key wrapped to the member's ECDH encryption key (Base64, same ephemeral-ECDH + AES-GCM envelope used for 1:1 payloads)."
          }
        }
      },
      "GroupKeySet": {
        "type": "object",
        "required": ["groupId", "epoch", "wrappedKey"],
        "description": "A single member's wrapped group key for one epoch — what the caller pulls to obtain a key it is missing.",
        "properties": {
          "groupId": {
            "type": "string",
            "format": "uuid",
            "description": "The group this key belongs to."
          },
          "epoch": {
            "type": "integer",
            "format": "int64",
            "description": "The epoch this wrapped key is for."
          },
          "wrappedKey": {
            "type": "string",
            "description": "The group epoch key wrapped to the caller's encryption key (Base64)."
          }
        }
      },
      "GroupKeyDistribution": {
        "type": "object",
        "required": ["epoch", "keys"],
        "description": "A complete set of wrapped group keys for one epoch, one entry per member. Generated client-side on create/rotation and posted to the Server, which relays the blobs and advances the roster epoch (compare-and-swap).",
        "properties": {
          "epoch": {
            "type": "integer",
            "format": "int64",
            "description": "The epoch these wrapped keys belong to. On rotation this must equal the group's current epoch + 1; the write is rejected (409) if the current epoch is not exactly the prior value."
          },
          "keys": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/WrappedGroupKey"
            },
            "description": "One wrapped key per member of the (post-change) roster. The Server validates the set covers exactly the roster."
          }
        }
      },
      "CreateGroupRequest": {
        "type": "object",
        "required": ["name", "members", "keys"],
        "description": "Creates a group with the caller as owner and an initial epoch-0 key set wrapped to every member.",
        "properties": {
          "name": {
            "type": "string",
            "description": "Group name (plaintext)."
          },
          "members": {
            "type": "array",
            "items": {
              "type": "string",
              "format": "uuid"
            },
            "description": "Initial member user ids (the owner is added automatically if omitted)."
          },
          "keys": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/WrappedGroupKey"
            },
            "description": "Epoch-0 group key wrapped to every initial member (including the owner)."
          }
        }
      },
      "UpdateMembersRequest": {
        "type": "object",
        "required": ["epoch", "keys"],
        "description": "Owner-only roster change. Adding and/or removing members always rotates: the body carries the new epoch's full wrapped-key set for the resulting roster. Compare-and-swap on epoch.",
        "properties": {
          "add": {
            "type": "array",
            "items": {
              "type": "string",
              "format": "uuid"
            },
            "description": "User ids to add to the roster."
          },
          "remove": {
            "type": "array",
            "items": {
              "type": "string",
              "format": "uuid"
            },
            "description": "User ids to remove from the roster."
          },
          "epoch": {
            "type": "integer",
            "format": "int64",
            "description": "The new epoch (must equal current epoch + 1). Rejected with 409 on mismatch."
          },
          "keys": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/WrappedGroupKey"
            },
            "description": "The new epoch key wrapped to exactly the resulting roster (after add/remove)."
          }
        }
      },
      "HubRegistrationRequest": {
        "type": "object",
        "description": "Sent by a Hub to the Server on boot to obtain its relay queue. Authentication is via the registration bearer token (a shared secret in Phase 1; a dashboard-issued per-Hub token later). The body itself carries no trust.",
        "properties": {
          "instanceId": {
            "type": "string",
            "description": "Optional Hub-chosen instance label reported for diagnostics/dashboard display only. The Server does not derive identity or trust from it; the authoritative hubId is assigned by the Server."
          }
        }
      },
      "HubRegistrationResponse": {
        "type": "object",
        "required": ["hubId", "queueName", "exchange"],
        "properties": {
          "hubId": {
            "type": "string",
            "description": "Server-assigned identifier for this Hub registration. Names the Hub's queue and (in the volunteer phase) ties it to an owner for accountability and revocation."
          },
          "queueName": {
            "type": "string",
            "description": "Name of the auto-delete queue the Server provisioned for this Hub. The Hub binds user routing keys to it and consumes it; the Hub never declares queues itself."
          },
          "exchange": {
            "type": "string",
            "description": "Name of the topic exchange the Hub binds user routing keys against (messages.exchange)."
          }
        }
      }
    }
  }
}